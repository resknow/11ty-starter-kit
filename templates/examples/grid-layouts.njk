---
title: Grid Layouts
---
{% from "macros/show-code/macro.njk" import showCode %}

<section class="block-area py-20" style="--row-gap: var(--space-20)">

    <header class="flow flow-space-2">
        <h1>Common Grid Layouts</h1>
        <p>Below are some common grid layouts. Check the source for the code for each of them.</p>
    </header>

    <main class="alignwide flow flow-space-32">

        <!-- 50/50 Split Grid -->
        {% set splitGrid5050 %}
            {%- include "examples/50-50-split-grid.njk" -%}
        {% endset %}
        {{ showCode(splitGrid5050) }}

        <!-- 30/70 Split Grid -->
        {% set splitGrid3070 %}
            {%- include "examples/30-70-split-grid.njk" -%}
        {% endset %}
        {{ showCode(splitGrid3070) }}

        <!-- 3 Column Grid -->
        {% set threeColumnGrid %}
            {%- include "examples/3-column-grid.njk" -%}
        {% endset %}
        {{ showCode(threeColumnGrid) }}

        <!-- 3 Column Grid -->
        {% set autoGrid %}
            {%- include "examples/auto-grid.njk" -%}
        {% endset %}
        {{ showCode(autoGrid) }}

    </main>

</section>